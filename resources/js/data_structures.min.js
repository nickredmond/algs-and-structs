function LinkedListNode(value) {
	this.value = value;
	this.next = null;
}

function LinkedList() {
	this.headNode = null;
}
LinkedList.prototype.insert = function(node, index) {
	if (index >= 0) {
		var currentIndex = 0;
		var lastNode = null;
		var currentNode = this.headNode;
		while (currentIndex !== index && currentNode !== null) {
			lastNode = currentNode;
			currentNode = currentNode.next;
			currentIndex += 1;
		}

		if (currentIndex === index) {
			if (lastNode) lastNode.next = node;
			node.next = currentNode;
		}
	}
};
LinkedList.prototype.add = function(node) {
	this.insert(node, this.size());
};
LinkedList.prototype.remove = function(node, index) {

};
LinkedList.prototype.size = function() {
	var size = 0;
	var node = this.headNode;

	while (node !== null) {
		size += 1;
		node = node.nextNode;
	}

	return size;
};
LinkedList.prototype.indexOf = function(value) {
	var index = -1;
	var node = this.headNode;
	var currentIndex = 0;

	while (node !== null && index === -1) {
		if (node.value ==== value) {
			index = currentIndex;
		}
		else {
			node = node.next;
			index += 1;
		}
	}

	return index;
};
LinkedList.prototype.toArray = function() {
	var array = [];
	var node = this.headNode;

	while (node !== null) {
		array.push(node.value);
		node = node.next;
	}

	return array;
};

var INITIAL_MEMORY_SIZE = 10;
var MEMORY_SCALAR = 2;
function Stack() {
	this.elements = new Array(INITIAL_MEMORY_SIZE);
	this.topIndex = -1;
};
/**
	NOTE: I am fully aware that JavaScript handles memory allocation internally, 
		and this function serves mainly as an imitation of how most runtime 
		environments may manage data structures "under-the-scenes".
*/
Stack.prototype.allocate_memory = function() {
	var existingElements = this.elements;
	this.elements = new Array(existingElements.length * MEMORY_SCALAR);
	for (var i = 0; i < existingElements.length; i++) {
		this.elements[i] = existingElements[i];
	}
};
Stack.prototype.push = function(value) {
	if (this.topIndex >= this.elements.length - 1) {
		this.allocate_memory();
	}
	this.topIndex += 1;
	this.elements[this.topIndex] = value;
};
Stack.prototype.pop = function() {
	var element = null;

	if (this.topIndex >= 0) {
		element = this.elements[this.topIndex];
		this.topIndex -= 1;
	}

	return element;
};